# 问题修复记录（对照《项目问题分析报告》+ 下一轮优化）

> 日期：2026-07-14（含当晚第三轮 + 第四轮 TTFT）  
> 对照报告：`项目问题分析报告.md`  
> 对照计划：智能客服下一轮优化 + **功能实测优化计划（第三轮 Phase 0–4）** + **第四轮 FAQ TTFT**  
> 验证：第二轮末 `pytest` **49 passed, 1 skipped**；第三轮末约 **81 passed**；第四轮后 **82 passed**（以当前 `tests/` 为准）  
> 说明：本文件供发给组员联调/Code Review；改动均为本地工作区，**尚未 commit**。

---

## 摘要

**第一轮（报告 P0→P2）**：公开入口不再下发 API Key；生产禁弱登录并强制 JWT 密钥；会话读删加 owner；流式不再二次 RAG；RemoteQwen / Docker / Alembic / sanitize / Redis / 结构化短路等已闭环。

**第二轮（优化计划 Phase 1–5）**：RAG WebSocket **真实 token 流**（ReAct 只规划，生成只做一次）；检索并行；Kimi timeout/温度/并发；共享 httpx；WS 缩短 DB 占用；LocalQwen GPU 锁；Agent 超时；complaint savepoint；生产禁用 `create_all`；WS 鉴权改 cookie；Redis WS 限流；`/health/deep` 鉴权与脱敏；Compose 端口收紧；Admin Docker 构建；Nginx gzip/安全头；前端 Cancel/退避/移动端。

**第三轮（功能实测优化计划 Phase 0–4，2026-07-14 晚）**：Kimi 温度统一（kimi-k2.6 → 1）；联调烟雾基线；RAG 后处理提质；WS 取消 / 会话 create-if-missing / 有用无用反馈；生产禁免密登录、Google `id_token` 验签、集成烟雾与 CI ruff 扩至 `scripts/`。

**第四轮（FAQ TTFT，2026-07-14 晚）**：运行时分段计时定位 WS FAQ 首包瓶颈；确认 kimi-k2.6 **默认 thinking** 在首个 `content` 前灌入大量 `reasoning_content`；默认关闭 thinking，按模式匹配 temperature；FAQ 首包约 **22s → ~3s**。

---

## 改动对照表 — 第一轮（原报告）

| 报告项 | 状态 | 主要文件 |
|--------|------|----------|
| P0-1 `/config.js` 泄露 API Key | ✅ | `src/main.py`, `frontend/app.js`, `frontend-admin/src/api.js` |
| P0-2 弱登录 / OAuth 未验签 / 默认 JWT | ✅ | `src/api/routes/auth.py`, `src/core/security.py` |
| P0-3 会话无所有权校验 | ✅ | `src/api/routes/chat.py`, `src/api/routes/feedback.py` |
| P1-4 `process_stream` 双次 RAG | ✅ | `src/services/agent_service.py` |
| P1-5 RemoteQwen 流式方法名 | ✅ | `src/services/llm/remote_qwen.py` |
| P1-6 Nginx / Docker 缺口 | ✅ | `docker/nginx.conf`, `Dockerfile.api`, `entrypoint.sh` |
| P1-7 Alembic 缺表 | ✅ | `alembic/versions/002_users_and_feedback.py` |
| P2-8 sanitize 误删反引号 | ✅ | `src/utils/text.py` |
| P2-9 Redis 静默失败 | ✅ | `src/redis_client.py`, `src/api/routes/health.py` |
| P2-10 结构化工具短路被删 | ✅ 已恢复 | `src/services/agent_service.py` |
| P2-11 测试盲区 | ✅ 部分补齐 | `tests/test_api.py`, `test_security.py`, `test_agent_service.py` |

---

## 改动对照表 — 第二轮（优化计划）

| Phase | 项 | 状态 | 主要文件 |
|-------|----|------|----------|
| 1.1 | 真实 RAG token 流（defer + `customer_chat_stream`） | ✅ | `agent_service.py`, `knowledge_chat.py` |
| 1.2 | 多语言检索 / Hybrid BM25+向量并行 | ✅ | `multilingual.py`, `retriever.py` |
| 1.3 | Kimi timeout/温度/semaphore + 共享 httpx | ✅ | `kimi_client.py`, `utils/http_client.py`, order/logistics/complaint/auth/remote_qwen |
| 2.1 | WS/REST：写消息后释放 DB，Agent 期间不占连接 | ✅ | `chat.py` |
| 2.2 | LocalQwen 流式与非流式共用 GPU lock | ✅ | `local_qwen.py` |
| 2.3 | Agent 超时；complaint `begin_nested`；WS 断开 aclose | ✅ | `agent_service.py`, `complaint.py`, `chat.py` |
| 2.4 | 生产禁用 lifespan `create_all` | ✅ | `main.py` |
| 3 | WS JWT 走 cookie；Redis WS 限流；生产匿名历史 cookie；health/deep 鉴权脱敏；compose 端口 | ✅ | `chat.py`, `health.py`, `docker-compose.yml`, `frontend/app.js` |
| 4 | Cancel/退避/移动端；Admin 多阶段 Docker build；Nginx gzip/头；`.env.example` | ✅ | `frontend/*`, `Dockerfile.api`, `nginx.conf`, `.env.example`, admin vite/api |
| 5 | 流式/检索/admin/health 回归；可选 integration skip；Qwen Protocol | ✅ | `tests/*`, `llm/protocol.py` |

---

## 行为变化（组员必读）

### 鉴权（含第二轮）

1. 公开聊天仍不要求 `X-API-Key`；管理端仍手动填 Key。
2. WebSocket **优先**用登录下发的 HttpOnly cookie（`JWT_COOKIE_NAME`）；前端**不再**把 JWT 放进 `?token=`（服务端仍短暂兼容 query）。
3. `ENV=production` 时：`/health/deep` 需要 `X-API-Key`；响应不再带完整 inference URL，且可省略 `env` 字段。
4. 生产环境下，**匿名**会话读/删历史需 cookie `kefu_anon_sids`（聊天成功后会 Set-Cookie）；开发环境仍可用 UUID 能力令牌。

### Agent / 流式

1. WS 流式：ReAct 对 `customer_chat` / `create_return_request` **只选型不生成**，随后走 `customer_chat_stream` 真 token 流（降低 TTFT）。
2. REST `process()` 仍一次完整生成。
3. 结构化订单/物流/投诉短路仍在；仅这些路径保留短切块。
4. Agent 整体超时默认 `AGENT_TIMEOUT_SECONDS=120`；Kimi 带 timeout + 并发限制（温度策略见第三轮 Phase 0，已不再用旧的 tools 0.2 / chat 0.7）。

### 部署 / 运维

1. Compose：Postgres/Redis **默认不映射宿主机端口**（需本地调试时自行取消注释）；Redis 开 AOF；`TRUSTED_PROXY=true`。
2. `Dockerfile.api` 用 Node 多阶段构建 `frontend-admin/dist`。
3. Nginx：gzip、基础安全头、转发 `X-Forwarded-For`。
4. 生产 schema **仅靠 Alembic**；开发仍可用 `create_all`。

### 前端

1. 取消按钮可用；WS 断线指数退避重连；窄屏 sidebar 纵向布局。
2. Admin `fetchHealthDeep` / `apiFetch` 正确序列化 FastAPI `detail`；Vite 代理含 `/health`、`/metrics`、`/inference`。

---

## 文件清单（第二轮增量为主）

```
src/services/agent_service.py
src/tools/knowledge_chat.py
src/tools/complaint.py
src/tools/order.py
src/tools/logistics.py
src/rag/multilingual.py
src/rag/retriever.py
src/services/llm/kimi_client.py
src/services/llm/local_qwen.py
src/services/llm/remote_qwen.py
src/services/llm/protocol.py
src/utils/http_client.py
src/api/routes/chat.py
src/api/routes/health.py
src/api/routes/auth.py
src/services/complaint_service.py
src/main.py
src/core/config.py
frontend/app.js
frontend/style.css
frontend/i18n.js
frontend-admin/src/api.js
frontend-admin/vite.config.js
docker/Dockerfile.api
docker/nginx.conf
docker/docker-compose.yml
.env.example
tests/test_agent_service.py
tests/test_api.py
tests/test_multilingual.py
tests/test_integration_smoke.py
问题修复记录.md
```

---

## 建议验证步骤

1. `/opt/anaconda3/envs/kefu/bin/python -m pytest tests/ -q` → **49 passed, 1 skipped**
2. WS 知识库问答：观察首包早于整段答完；Network 里 WS URL **无** `?token=`
3. 登录后确认 cookie `kefu_token` 存在，刷新页面仍可识别登录态
4. 管理后台填 API Key 后能打开仪表盘 deep health；无 Key 时 production deep 应 401
5. Docker build API 镜像应产出 `/admin` 静态资源；8080 访问带 gzip/安全头

---

## 改动对照表 — 第三轮（功能实测优化计划，2026-07-14 晚）

| Phase | 项 | 状态 | 主要文件 / 结果 |
|-------|----|------|-----------------|
| 0 | kimi-k2.6 强制 `temperature=1`；可读 4xx `RuntimeError` | ✅ | `kimi_client.py`, `tests/test_kimi_client.py` |
| 0 | `MOONSHOT_TEMPERATURE`（默认 1）、`MOONSHOT_FIXED_TEMPERATURE_MODELS`；移除旧 tools 0.2 / chat 0.7 | ✅ | `config.py`, `.env.example` |
| 1 | 联调烟雾基线（Phase 0 后）：REST 11/11，0 次温度 500 | ✅ | 实测 |
| 1 | WS FAQ：TTFT ~9s，真 chunk 流，tools+cites | ✅ | 实测 |
| 1 | Offline `eval_agent` 13/13；live `select_tool` 9/10（90%） | ✅ | `scripts/eval_agent.py` |
| 1 | `golden_qa.jsonl` 扩样（logistics / my_orders / 退货多轮 / en FAQ） | ✅ | `data/eval/golden_qa.jsonl` |
| 2 | RAG 后处理：reject / diversity / gulf expand·boost | ✅ | `src/rag/postprocess.py`（新） |
| 2 | 接入 retriever / multilingual / knowledge_chat；`RAG_MIN_SCORE=0.60`、`RAG_MAX_PER_SOURCE=2` | ✅ | 对应模块 + `config.py` |
| 2 | `eval_rag`：hit 100%、recall@3 100%、negative precision 100%（6/6） | ✅ | 测评；`tests/test_rag_postprocess.py` |
| 3 | WS `{type:cancel}` 在消息校验前生效；`aclose` 流；`{type:cancelled}` | ✅ | `chat.py`, `agent_service.py` |
| 3 | REST/WS：合法未知 UUID create-if-missing；新会话发 `session_id=null` | ✅ | `chat.py`, `session_service.py`, `frontend/app.js` |
| 3 | 轻量有用/无用 → `/api/v1/chat/feedback` | ✅ | `chat.py`, `frontend/app.js`, `i18n.js`, `style.css` |
| 4 | 生产禁免密登录；需验签时用 `google-auth` 校验 Google `id_token` | ✅ | `auth.py`, `security.py`, `pyproject.toml` |
| 4 | 集成烟雾：Alembic + Redis（testcontainers 或本地） | ✅ | `tests/test_integration_smoke.py` |
| 4 | CI ruff 含 `scripts/` | ✅ | `.github/workflows/ci.yml` |

---

### 行为变化（第三轮，组员必读）

#### Kimi / 配置

1. `kimi-k2.6`（及 `MOONSHOT_FIXED_TEMPERATURE_MODELS` 中的模型）请求温度固定为 **1**；全局默认 `MOONSHOT_TEMPERATURE=1`。
2. 已移除旧的「tools 0.2 / chat 0.7」分场景默认；Kimi 4xx 会抛出可读的 `RuntimeError`。

#### RAG

1. 检索结果经 `postprocess`：低分拒绝、来源多样性、`gulf` 扩召回/加权。
2. 默认阈值：`RAG_MIN_SCORE=0.60`，`RAG_MAX_PER_SOURCE=2`。

#### 会话 / UX

1. WS 可发 `{type:cancel}`（不必等 message 校验通过）；服务端 aclose 流并回 `{type:cancelled}`。
2. REST/WS 对合法但未知的 `session_id` UUID 会 create-if-missing；前端新建会话传 `session_id=null`。
3. 助手消息可点「有用/无用」，提交至 `/api/v1/chat/feedback`。

#### 安全 / 运维

1. `ENV=production` 时拒绝免密登录。
2. Google 登录在需要验签时走 `google-auth` 校验 `id_token`。
3. 集成烟雾覆盖 Alembic + Redis；CI ruff 扫描含 `scripts/`。

---

### 文件清单（第三轮增量）

```
src/services/llm/kimi_client.py
src/core/config.py
src/rag/postprocess.py
src/rag/retriever.py
src/rag/multilingual.py
src/tools/knowledge_chat.py
src/api/routes/chat.py
src/api/routes/auth.py
src/core/security.py
src/services/agent_service.py
src/services/session_service.py
frontend/app.js
frontend/i18n.js
frontend/style.css
.env.example
data/eval/golden_qa.jsonl
tests/test_kimi_client.py
tests/test_rag_postprocess.py
tests/test_integration_smoke.py
.github/workflows/ci.yml
pyproject.toml
问题修复记录.md
```

---

### 建议验证步骤（第三轮）

1. `/opt/anaconda3/envs/kefu/bin/python -m pytest tests/ -q` → 约 **81 passed**（mock；integration 视环境 skip/跑）
2. REST 烟雾：消息接口无 temperature 相关 500
3. WS FAQ：可见真实 chunk；可发 cancel 收到 `cancelled`
4. 新建聊天不复用旧 UUID（`session_id=null`）；未知合法 UUID 可自动建会话
5. 有用/无用反馈能打到 `/api/v1/chat/feedback`
6. `eval_rag` / offline `eval_agent` 保持上表量级；live tool 选型约 9/10
7. 生产配置下免密登录应被拒；Google token 验签路径可用

---

## 改动对照表 — 第四轮（FAQ TTFT / kimi thinking）

| 项 | 状态 | 主要文件 / 结果 |
|----|------|-----------------|
| 分段计时（ReAct / 检索 / 流式首 token） | ✅ 已用于定位并清理 | 曾埋点 `agent_service` / `knowledge_chat` / `retriever` / `kimi_client` |
| 根因：默认 thinking 拖慢首 content | ✅ | 流打开后约 **275** 个无 content chunk，`ms_since_create≈16.7s` |
| `MOONSHOT_THINKING`（默认 `disabled`） | ✅ | `config.py`, `.env.example`, `kimi_client.py`（`extra_body.thinking`） |
| 温度随 thinking：enabled→1.0 / disabled→0.6 | ✅ | `_resolve_temperature`；对齐 Moonshot 文档 |
| tool 多轮保留 `reasoning_content` | ✅ | `build_assistant_tool_message*`（thinking=enabled 时多步工具需要） |
| 单测覆盖 thinking / 温度 | ✅ | `tests/test_kimi_client.py`（→ **82 passed**） |

### 行为变化（第四轮，组员必读）

1. 默认 **`MOONSHOT_THINKING=disabled`**：请求带 `thinking: {type: disabled}`，temperature **0.6**；FAQ WS 首包约 **~3s**（实测客户端 ~3.1s，整轮 ~4.3s）。
2. 需要深度推理时设 **`MOONSHOT_THINKING=enabled`**（temperature **1.0**）；首包会明显变慢。
3. 此前「TTFT ~9s」记录偏保守；以 cloud RAG + 默认 thinking 实测，主耗时在 **RAG 流式 reasoning**，而非仅 ReAct。

### 运行时证据摘要（FAQ：「手机保修多久？」）

| 阶段 | 修复前 | 修复后 |
|------|--------|--------|
| ReAct | ~3.7s | ~1.4s |
| 检索（热身） | ~0.3s | ~0.3s |
| 流打开 → 首 content | **~16.7s**（275 chunks） | **~6ms**（3 chunks） |
| 客户端 TTFT | **~22.6s** | **~3.1s** |

### 文件清单（第四轮增量）

```
src/core/config.py
src/services/llm/kimi_client.py
.env.example
tests/test_kimi_client.py
问题修复记录.md
```

### 建议验证步骤（第四轮）

1. `/opt/anaconda3/envs/kefu/bin/python -m pytest tests/ -q` → **82 passed**
2. WS FAQ「手机保修多久？」：首包约 **3–5s**，答案仍含保修年限要点
3. 可选：`MOONSHOT_THINKING=enabled` 重启后同问，确认首包明显变慢（对照）

---

## 未做 / 后续可跟进

- ~~CI 对 `scripts/` 的 ruff 范围仍可对齐~~ → **第三轮已做**
- ~~testcontainers / 本地 Postgres+Redis+Alembic 集成烟雾~~ → **第三轮已做**（环境不具备时仍可能 skip）
- ~~换 `google-auth` 本地验签 Google `id_token`~~ → **第三轮已做**
- ~~WS FAQ TTFT（thinking 默认开启导致 ~20s+）~~ → **第四轮已做**（默认关闭 thinking → ~3s）
- Google OAuth **前端 UI** 仍可完善（后端验签已就绪）
- ReAct 规划仍约 **~1–1.5s**；可用 FAQ 启发式短路再压（可选）
- 生产强制登录聊天或短期 WS ticket（目前 cookie + 可选匿名）
- 匿名会话 cookie 仅 REST 落盘、WS 路径未 Set-Cookie；反馈可再绑 `message_id`
