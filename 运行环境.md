# 项目运行环境

本项目统一使用以下 Python 解释器，**安装依赖、跑脚本、启动服务时一律用它**，不要用系统 Python、Homebrew Python 或其它 conda 环境。

## Python

| 项 | 值 |
|----|-----|
| 解释器 | `/opt/anaconda3/envs/kefu/bin/python` |
| Conda 环境名 | `kefu` |
| 版本 | Python 3.10 |

## 常用命令

```bash
# 激活（可选；也可直接写完整路径）
conda activate kefu

# 安装依赖
/opt/anaconda3/envs/kefu/bin/pip install -e ".[lint,test]"

# 启动 API（macOS 上若遇 OpenMP 冲突需带上该环境变量）
export KMP_DUPLICATE_LIB_OK=TRUE
/opt/anaconda3/envs/kefu/bin/python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

# 跑测试 / 脚本
/opt/anaconda3/envs/kefu/bin/python -m pytest tests -v
/opt/anaconda3/envs/kefu/bin/python scripts/rebuild_index.py
```

## 给 Agent 的约定

- 所有 Python / pip / uvicorn / pytest 命令必须走 `/opt/anaconda3/envs/kefu/bin/`
- 不要新建 `.venv`，不要改用 `python3` / `/opt/homebrew/bin/python3.13`
