# 智能客服项目问题分析报告

> 审查日期：2026-07-14  
> 范围：后端核心、配置与部署、鉴权与会话、RAG/Agent、前端与测试  
> 验证（审查时）：`pytest` 39 passed；工作区仅有未提交改动 `src/services/agent_service.py`  
> **修复跟踪**：见同目录 [`问题修复记录.md`](./问题修复记录.md)（2026-07-14，`pytest` 46 passed）

本报告只收录有代码证据、会影响安全、正确性或运维的问题，按优先级排序。

---

## 摘要

项目测试全部通过，但大量依赖 mock，掩盖了生产级风险。当前最严重的是：**API Key 通过 `/config.js` 泄露**、**登录/OAuth 可伪造身份**、**会话无所有权校验**；功能层面突出的是 **WebSocket 流式 RAG 重复执行**、**RemoteQwen 流式接口不兼容**、**Docker/Nginx 部署缺口**、**Alembic 迁移落后于模型**。

---

## P0 — 严重（安全 / 数据）

### 1. `/config.js` 明文下发 API Key

**位置：** `src/main.py`（约 122–131 行）

```python
@app.get("/config.js")
async def frontend_config_js():
    payload = {
        "apiKey": settings.api_key or "",
        "wsPath": "/api/v1/ws/chat",
    }
```

**触发条件：** 任意访客访问 `/config.js`。

**影响：**

- 浏览器前端与管理后台共用同一 `X-API-Key`。
- 拿到 Key 后可调用聊天、上传/重建知识库、改投诉状态等受保护接口。
- 生产即使换掉默认 Key，Key 仍被公开分发，等同于未鉴权。

**建议：**

- 停止在公开静态配置里下发真实 API Key。
- 管理端与公开聊天鉴权分离（短生命周期 token / 会话 Cookie / 角色权限）。
- 前端 WebSocket 鉴权改为 HttpOnly Cookie 或短暂 WS ticket。

---

### 2. 认证可伪造，JWT 默认密钥未强制校验

**位置：**

- `src/api/routes/auth.py`（`/login`、`/oauth`）
- `src/core/config.py`（`jwt_secret` 默认值）
- `src/core/security.py`（`validate_settings_on_startup` 仅检查 `API_KEY`）

**问题描述：**

| 问题点 | 说明 |
|--------|------|
| 无密码登录 | `POST /api/v1/auth/login` 仅凭邮箱即可签发 JWT |
| OAuth 未验签 | Google `id_token` 仅打日志，任意 `email`/`subject` 即可建用户拿 token |
| 弱 JWT 密钥 | 默认 `change-me-jwt-secret`；生产启动不强制拒绝 |

**影响：** 攻击者可冒充任意用户，查看「我的订单」、绑定会话归属，扩大数据面。

**建议：**

- 生产禁用密码less email login，或接入真实身份校验。
- OAuth 真正校验 `id_token`（如 google-auth）。
- `ENV=production` 时拒绝默认 `JWT_SECRET`。

---

### 3. 会话历史读/删无所有权校验

**位置：** `src/api/routes/chat.py`（`GET/DELETE /api/v1/sessions/{session_id}`）

**触发条件：** 持有 API Key（或配合问题 1 公开拿到 Key）且知道/枚举到 session UUID。

**影响：** 可读取或删除任意用户对话历史，构成横向越权。

**建议：**

- 读删会话必须校验当前登录用户（或签发时的 owner）。
- 匿名会话使用不可猜测的 secret + 短期凭证，而不是仅靠 UUID + 全局 API Key。

---

## P1 — 高（功能正确性 / 部署）

### 4. WebSocket 流式路径对 RAG 重复执行

**位置：** `src/services/agent_service.py` → `process_stream`

**链路：**

1. `_run_react` → `_call_tool` 已完整执行一次 `customer_chat` / `create_return_request`
2. 随后又调用 `customer_chat_stream` 再次检索 + 生成

**影响：**

- 延迟与 LLM/检索成本约翻倍。
- REST `process()` 复用 tool 结果，WebSocket 不复用，两条路径行为不一致。

**建议：** 流式路径直接播已有 tool 结果，或 ReAct 阶段只规划、真正生成只做一次（stream）。

---

### 5. `RemoteQwenClient` 流式方法名不兼容

**位置：**

- 本地：`LocalQwenService.generate_from_messages_stream`（`src/services/llm/local_qwen.py`）
- 远程：`RemoteQwenClient.generate_stream`（`src/services/llm/remote_qwen.py`）
- 调用方：`src/tools/knowledge_chat.py` 统一调用 `generate_from_messages_stream`

**触发条件：** `RAG_BACKEND=local` 且配置了 `QWEN_INFERENCE_URL`，走流式 RAG。

**影响：** `AttributeError`，Docker GPU profile 下远程推理流式链路易断。

**建议：** 统一方法签名（两端都实现同名流式接口），远程无真流式时可包装成伪流式。

---

### 6. Nginx / Docker 部署缺口

**位置：** `docker/nginx.conf`、`docker/docker-compose.yml`、`docker/Dockerfile.api`

| 问题 | 影响 |
|------|------|
| Nginx 只反代 `/api/`、`/health`、`/metrics`，未代理 `/config.js` | 经 8080 访问时前端拿不到运行时配置 |
| compose 未构建/挂载 `frontend-admin` | 全栈 Docker 下 `/admin` 基本不可用 |
| API Dockerfile `pip install -e .` 早于完整源码 COPY | editable 安装在部分构建场景下可能失败 |

**建议：**

- Nginx 增加 `/config.js`（或取消依赖该公开 Key 下发）。
- 将 `frontend-admin/dist` 纳入镜像或静态挂载。
- 调整 Docker 构建顺序，确保安装时包源完整。

---

### 7. Alembic 迁移落后于 ORM 模型

**位置：** `alembic/versions/001_initial_schema.py` vs `src/models/`

**现状：**

- 迁移包含：`sessions`、`messages`、`complaints`、`knowledge_docs`
- ORM 另有：`users`、`message_feedback`
- `docker/entrypoint.sh`：`alembic upgrade head || echo skipped`，再依赖启动时 `create_all`

**影响：** 仅执行 Alembic、不做 `create_all` 的环境会缺表；迁移历史与真实 schema 不一致，后续变更难跟踪。

**建议：** 补充 migration；生产以 Alembic 为唯一真源，`create_all` 仅保留开发便利。

---

## P2 — 中（体验 / 可靠性）

### 8. `sanitize_assistant_reply` 误删反引号内容

**位置：** `src/utils/text.py`（`_THINK_BLOCK_RE` 含 `` `[\s\S]*?` ``）

**影响：** 订单号、路径、短代码等被反引号包裹的合法内容会被剥掉，用户可见答案异常截断。

**建议：** 仅匹配明确的 think / reasoning 标签，不要用裸反引号做通配清理。

---

### 9. Redis 失败被静默吞掉

**位置：** `src/redis_client.py`（连接失败返回 `None`）

**影响：**

- 会话历史缓存、滚动摘要、退货 Workflow 状态静默失效。
- `/health/deep` 将 Redis 标为 optional，但业务逻辑强依赖它。

**建议：** 生产启动对 Redis 做 soft/hard fail 策略；Workflow 不可用不静默降级为“看似正常”。

---

### 10. 未提交改动：结构化工具回复短路被移除

**位置：** 工作区 diff — `src/services/agent_service.py`

原先对 `query_order` / `query_my_orders` / `fetch_logistics_information` / `record_user_complaint` 可本地格式化回复，避免等待 Kimi 合成；现已删除，一律走 `synthesize_*`。

**影响：** 订单/物流等场景首包延迟可能显著变长（原注释提及数十秒量级），失败时才 fallback。

**建议：** 确认是有意性能权衡还是误删；若强调首包延迟，应恢复或做成可配置开关。

---

### 11. 测试与 CI 盲区

**验证：** `pytest` 39 passed；测试 fixture 大量 mock Agent。

**缺口示例：**

- 无 OAuth / 会话所有者鉴权测试
- 无 `process_stream` 双次 RAG 回归
- 无 `/config.js` 泄露相关断言
- 无 RemoteQwen 接口兼容测试
- 无 Alembic vs ORM 完整性检查
- 本地 `ruff check src tests scripts` 可在 `scripts/` 报 E402；CI 未扫 scripts

---

## 其他次要问题（备忘）

| 项 | 说明 |
|----|------|
| WS 限流用进程内 dict + `client.host` | 多实例不共享；经代理时可能都变成同一 IP |
| `query_order` 在缺少真实 Order API 时返回 mock | 开发可用，生产未配置会直接失败（已有保护） |
| `.env.example` 中 `QWEN_MODEL_PATH` | 与 README 默认 `Qwen3.5-2B` 不一致（示例为 0.8B） |
| `frontend-admin` 本地 build | 曾出现 `vite` 可执行权限不足（Permission denied） |

---

## 建议修复顺序

| 优先级 | 行动项 |
|--------|--------|
| P0 | 停下发真实 API Key；管理/聊天鉴权分离；会话读写加 owner 校验 |
| P0 | 生产禁用弱登录路径；校验 OAuth；强制非默认 `JWT_SECRET` |
| P1 | 修复 `process_stream` 重复 RAG |
| P1 | 统一 Remote/Local Qwen 流式接口；补 Nginx `/config.js` 与 admin 静态资源 |
| P1 | 补齐 Alembic（`users`、`message_feedback`） |
| P2 | 收紧 sanitize；明确 Redis 依赖策略；评估恢复结构化工具短路 |
| P2 | 补安全与流式回归测试；对齐 CI lint 范围 |

---

## 附录：架构简图

```
用户 (Web / API / Admin)
       │
       ▼
  FastAPI (src/main.py)
       │
       ▼
  AgentService
       ├── 问候短路
       ├── 退换货 Workflow（Redis）
       └── ReAct（Kimi）→ 工具 / RAG
              │
              ▼
  PostgreSQL + Redis + Chroma（可选本地/远程 Qwen）
```

审查时项目自称 production-ready，但上述 P0/P1 问题表明：**在公开部署前必须先完成鉴权与会话隔离加固**。
